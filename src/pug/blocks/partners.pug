section.section.container
  include ../partials/section-heading
  .partners-wrapper.swiper
    .partners.swiper-wrapper
      each val,index in [1,2,3,4,5]
        .partners__item.swiper-slide
          img.partners__logo(src=require(`@/images/partners/${index + 1}.png`), alt="")
